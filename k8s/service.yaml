apiVersion: v1
kind: Service
metadata:
  name: portfolio-api
  namespace: portfolio
  labels:
    app: portfolio
    component: api
spec:
  type: ClusterIP
  selector:
    app: portfolio
    component: api
  ports:
    - name: http
      port: 3000
      targetPort: 3000
